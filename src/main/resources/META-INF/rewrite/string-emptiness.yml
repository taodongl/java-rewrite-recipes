#
# String emptiness modernization recipe.
#
# String.isEmpty() exists since Java 6, so this recipe carries no version gate.
#
---
type: specs.openrewrite.org/v1beta/recipe
name: io.github.taodongl.UseStringIsEmpty
displayName: Use `String.isEmpty()`
description: >-
  Replace `String.length()` comparisons against zero and `equals("")` checks with `String.isEmpty()`.
  Safe on any Java version (`String.isEmpty()` was introduced in Java 6).
recipeList:
  - io.github.taodongl.StringIsEmptyCanBeUsedRecipes
